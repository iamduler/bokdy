version: "2"
sql:
  - engine: "postgresql"
    queries: "internal/platform/persistence/queries"
    schema: "migrations"
    gen:
      go:
        package: "dbsqlc"
        out: "db/generated/sqlc"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_interface: true
        emit_empty_slices: true
        overrides:
          - db_type: "uuid"
            go_type: "github.com/google/uuid.UUID"
